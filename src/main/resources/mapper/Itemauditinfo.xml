<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.cq.ItemauditinfoDao">
	<select id="queryIteminfo" resultType="java.util.Map">
		select  * from iteminfo i1,Personinfo p1 where i1.user_id=p1.user_id
	</select>
	<!-- <select id="queryCon" resultType="com.entity.util.Itemauditinfo">
		select  con_id,con_name from consumer where con_user=#{conUser}
	</select>
	<insert id="addAuditinfo" parameterType="com.entity.util.Itemauditinfo">
		insert into dept values(#{item_id},#{audit_id},#{audit_advice},#{isno},#{audit_cause},#{audit_time})
	</insert>
	<select id="queryIteminfo" resultType="java.util.Map">
		select * from iteminfo
	</select> -->
</mapper> 